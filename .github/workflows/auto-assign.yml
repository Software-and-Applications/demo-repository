name: Auto Assign
on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]
jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: 'Auto-assign issue'
      uses: pozil/auto-assign-issue@v1
      with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          assignees: tabrezahmed51
          numOfAssignee: 1
          assignees: tabrezahmed51, anotherUser
          numOfAssignee: 2
      with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          assignees: tabrezahmed51
          numOfAssignee: 1
          label: "auto-assign"
          jobs:
  assign-issue:
    if: github.event_name == 'issues'
    ...
  assign-pr:
    if: github.event_name == 'pull_request'
    ...



